<?xml version="1.0" encoding="utf-8"?>
<!-- 러닝 페이스 개선 배지. 성공 색 텍스트를 얹으므로 배경은 같은 계열의
     옅은 톤과 얇은 테두리만 쓴다. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/widget_success_soft" />
    <stroke android:width="1dp" android:color="@color/widget_success_border" />
    <corners android:radius="999dp" />
</shape>
